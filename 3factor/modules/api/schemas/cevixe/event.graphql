
interface Event {
    _id: ID!
    _type: String!
    _time: AWSDateTime!
    _author: String!
    _sourceId: ID!
    _sourceType: String!
    _sourceVersion: Int!
    _transaction: String!
}

extend type Message {
    event: Event!
}
