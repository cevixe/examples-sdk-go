
extend type Query {
    entityVersions(type: String!, id: ID!): VersionMetadataPage!
}

type VersionMetadataPage {
    items: [VersionMetadata]!
    nextToken: String
}

type VersionMetadata {
    time: AWSDateTime!
    author: String!
    version: Int!
}